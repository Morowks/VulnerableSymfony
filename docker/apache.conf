<Directory /app/public>
	# FIXED: Directory listing — "-Indexes" disables automatic directory
	# listings so the contents of a directory without an index file are no
	# longer exposed.
	Options -Indexes +FollowSymLinks
	AllowOverride All
	Require all granted
</Directory>

ServerTokens Prod
ServerSignature Off
